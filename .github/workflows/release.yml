name: Build and Release

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - uses: denoland/setup-deno@v2
        with:
          deno-version: ^2.9

      - name: Build desktop binary
        run: deno desktop -A main.ts

      - name: Make binary executable
        if: runner.os == 'Linux'
        run: chmod +x "$(basename "$PWD")/$(basename "$PWD")"

      - name: Archive build output
        shell: bash
        run: |
          APP_NAME="$(basename "$PWD")"
          TARGET_OS="$(echo "${{ runner.os }}" | tr '[:upper:]' '[:lower:]')"
          TARGET_ARCH="$(echo "${{ runner.arch }}" | tr '[:upper:]' '[:lower:]')"
          ZIP_NAME="${APP_NAME}-${TARGET_OS}-${TARGET_ARCH}.zip"
          if [ "${{ runner.os }}" = "Windows" ]; then
            7z a "${ZIP_NAME}" "${APP_NAME}/"
          else
            zip -r "${ZIP_NAME}" "${APP_NAME}/"
          fi

      - name: Upload binary artifact
        uses: actions/upload-artifact@v4
        with:
          name: binary-${{ runner.os }}-${{ runner.arch }}
          path: "*.zip"

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: List artifacts
        run: find artifacts -type f

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ github.run_number }}
          name: Release v${{ github.run_number }}
          body: |
            Automated build from main branch.
          files: artifacts/**/*.zip
